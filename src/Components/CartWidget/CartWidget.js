import cart from './Assets/cart.svg';

const CartWidget = () => {
    return (
        <div>
            <img src={cart} alt="Icono del Carrito" />
            5
        </div>
    )
}

export default CartWidget;